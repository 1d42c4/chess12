import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const esc=s=>String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const series=[['chess11','See the Board','Notice threats and legal moves'],['chess12','Calculate with Purpose','Find and verify tactics'],['chess13','Make a Plan','Turn positional clues into decisions'],['chess14','Finish the Game','Convert advantages and hold draws']];
const file=l=>`lesson-${String(l.number).padStart(2,'0')}.html`;
const pieceNames={p:'pawn',n:'knight',b:'bishop',r:'rook',q:'queen',k:'king'};
function boardFrame(board){return `<div class="board-frame"><div class="rank-labels" aria-hidden="true">${[8,7,6,5,4,3,2,1].map(r=>`<span>${r}</span>`).join('')}</div>${board}<div class="file-labels" aria-hidden="true">${[...'abcdefgh'].map(f=>`<span>${f}</span>`).join('')}</div></div>`;}
function staticBoard(fen){let out='',rank=8;for(const row of fen.split(' ')[0].split('/')){let f=0;for(const ch of row){if(/[1-8]/.test(ch)){for(let n=0;n<+ch;n++,f++)out+=`<div class="sq${(f+rank)%2!==0?' dark':''}" role="img" aria-label="${'abcdefgh'[f]}${rank}: empty"></div>`;}else{const white=ch===ch.toUpperCase(),square='abcdefgh'[f]+rank;out+=`<div class="sq${(f+rank)%2!==0?' dark':''}" role="img" aria-label="${square}: ${white?'White':'Black'} ${pieceNames[ch.toLowerCase()]}"><img class="piece-svg" src="assets/pieces/${white?'w':'b'}${ch.toUpperCase()}.svg" alt="" draggable="false"></div>`;f++;}}rank--;}return boardFrame(`<div class="board" role="group" aria-label="Chess position, White at bottom">${out}</div>`);}
export function renderSite(C,root){
 fs.mkdirSync(root,{recursive:true});fs.mkdirSync(path.join(root,'downloads'),{recursive:true});
 const pages=`https://knightway8.github.io/${C.repo}/`,repo=`https://github.com/knightway8/${C.repo}`;
 const write=(name,s)=>fs.writeFileSync(path.join(root,name),s);
 const nav=active=>`<a class="skip" href="#main">Skip to content</a><header class="top"><div class="wrap"><a class="brand" href="index.html"><span class="mark" aria-hidden="true">♞</span><span><small>The Human Chess Project</small><strong>${C.title}</strong></span></a><nav aria-label="Main navigation">${[['index.html','Learn'],['practice.html','Practice'],['study-plan.html','Study plan'],['downloads.html','Downloads'],['sources.html','Sources']].map(([url,title])=>`<a href="${url}"${url===active?' aria-current="page"':''}>${title}</a>`).join('')}</nav></div></header>`;
 const footer=`<footer><div class="wrap"><span>Built for human learners · Original AI-created lessons · Free to read</span><span><a href="${repo}">Repository & README</a> · <a href="sources.html">Sources and verification</a> · <a href="progress.html">Your progress</a></span></div></footer>`;
 function page(title,body,name,extra=''){return `<!doctype html><html lang="en" style="--accent:${C.color}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${esc(C.promise)}"><meta name="theme-color" content="${C.color}"><link rel="canonical" href="${pages}${name==='index.html'?'':name}"><link rel="icon" type="image/svg+xml" href="assets/favicon.svg"><link rel="stylesheet" href="assets/style.css"><title>${esc(title)} · ${C.title}</title></head><body>${nav(name)}<main id="main" class="wrap">${body}</main>${footer}<script src="vendor/chess.js"></script><script src="assets/data.js"></script>${extra}<script src="assets/app.js"></script></body></html>`;}
 const progress=`<div class="progress-track" aria-hidden="true"><div class="progress-fill" data-progress-fill></div></div><p class="progress-note" data-progress-text></p>`;
 const seriesHTML=`<div class="series">${series.map(([n,t,s])=>`<a href="https://knightway8.github.io/${n}/"><span>${n} · ${n===C.repo?'YOU ARE HERE':'COMPANION COURSE'}</span><strong>${t}</strong><span>${s}</span></a>`).join('')}</div>`;
 const modules=C.modules.map((m,i)=>`<section class="card module-card"><span class="module-number">MODULE ${String(i+1).padStart(2,'0')}</span><h3>${m}</h3><ul class="lesson-list">${C.lessons.filter(l=>l.module===i).map(l=>`<li data-lesson-id="${l.id}"><a href="${file(l)}"><span class="num">${String(l.number).padStart(2,'0')}</span><span class="name">${esc(l.title)}</span><span class="done-mark" aria-label="Completion status"></span><span aria-hidden="true">↗</span></a></li>`).join('')}</ul></section>`).join('');
 const hero=C.drills.find(d=>d.kind==='move')||C.drills[0];
 write('index.html',page(C.title,`<section class="hero"><div><p class="eyebrow">COURSE ${+C.repo.replace('chess','')-10} OF 4 · LEARN, TRY, EXPLAIN</p><h1>${C.title}.</h1><p class="lead">${C.subtitle}. ${C.promise}</p><div class="actions"><a class="button" href="lesson-01.html">Start lesson 01 →</a><a class="button secondary" href="practice.html">Open practice lab</a></div><p class="micro">No account. No clock unless you want one. Your progress stays on your browser.</p></div><div class="hero-board">${staticBoard(hero.fen)}<p>${hero.fen.split(' ')[1]==='w'?'White':'Black'} to move · A position from this course’s practice lab</p></div></section><div class="statbar"><div><strong>24</strong><span>distinct lessons</span></div><div><strong>${C.drills.length}</strong><span>interactive exercises</span></div><div><strong>6 weeks</strong><span>suggested study route</span></div><div><strong>Offline</strong><span>download once, keep learning</span></div></div><section><div class="section-head"><div><p class="eyebrow">THE COURSE</p><h2>One useful idea at a time.</h2><p>${C.audience}</p></div><label class="search"><span class="micro">Find a lesson</span><input id="search" type="search" placeholder="Search a concept or question…" aria-label="Search lessons"></label></div>${progress}<p id="search-status" class="status" role="status">24 lessons shown</p><div class="grid">${modules}</div></section><section class="banner"><h2>Understanding is something you practice.</h2><p>Read one lesson. Try its task before revealing the answer. Work a few unfamiliar positions. Then look for the same cue in a slow game.</p><a class="button" href="study-plan.html">Follow the six-week route →</a></section><section><p class="eyebrow">THE WHOLE PATH</p><h2>Four skills. One game.</h2>${seriesHTML}</section>`,'index.html'));
 const toc=current=>`<aside class="toc" aria-label="Course lessons">${C.modules.map((m,i)=>`<details ${C.lessons.find(l=>l.id===current)?.module===i?'open':''}><summary>${i+1}. ${m}</summary>${C.lessons.filter(l=>l.module===i).map(l=>`<a class="${l.id===current?'current':''}" href="${file(l)}">${String(l.number).padStart(2,'0')} ${esc(l.title)}</a>`).join('')}</details>`).join('')}<a href="practice.html">Open practice lab →</a><a href="workbook.html">Printable workbook →</a></aside>`;
 for(const l of C.lessons){const next=C.lessons[l.number],prev=C.lessons[l.number-2],d=C.drills[(l.number-1)*Math.floor(C.drills.length/24)];const content=`<div class="page-intro"><p class="eyebrow">${C.modules[l.module]} · LESSON ${String(l.number).padStart(2,'0')} / 24</p><h1>${esc(l.title)}</h1><p class="lead">${esc(l.goal)}</p></div><div class="reading-layout">${toc(l.id)}<article class="prose"><p class="micro">About 10–15 minutes with the task · Read, attempt, then explain</p><h2>The idea</h2><p>${esc(l.teach)}</p><section class="callout"><h3>Worked explanation</h3><p>${esc(l.example)}</p></section>${l.diagram?`<figure class="model-diagram">${staticBoard(l.diagram.fen)}<figcaption>${esc(l.diagram.caption)}</figcaption></figure>`:''}<h2>Try it yourself</h2><p>${esc(l.task)}</p><details class="answer"><summary>Reveal the task answer and reasoning</summary><p>${esc(l.answer)}</p></details><section class="callout trap"><h3>The mistake to watch for</h3><p>${esc(l.trap)}</p></section><details class="answer"><summary>Close the lesson and test your recall</summary><p>${esc(l.recall)}</p></details><h2>Apply it to an unfamiliar board</h2><p>Use the practice lab to inspect a fresh position. Its recorded answer checks a specific move, count, or result; your job is also to explain the reason. The lab mixes skills across the course.</p><a class="button" href="practice.html?id=${d.id}">Try practice exercise ${String((l.number-1)*Math.floor(C.drills.length/24)+1).padStart(3,'0')} →</a><h2>Bring it into a real game</h2><p>In your next slow game, look for a moment where this lesson’s idea applies. Record the position, the cue you noticed or missed, and what you would inspect next time.</p><label class="journal-label" for="lesson-notes">Your explanation and game notes</label><textarea id="lesson-notes" placeholder="Position or move number → what I noticed → what I will check next time"></textarea><p id="note-status" class="status">Notes stay on this browser. Export them from Your progress.</p><div class="actions"><button id="complete-lesson" type="button">Mark lesson complete</button><button class="secondary" data-export>Export progress & notes</button></div>${progress}<div class="lesson-nav">${prev?`<a href="${file(prev)}">← ${esc(prev.title)}</a>`:'<a href="index.html">← Course home</a>'}${next?`<a href="${file(next)}">${esc(next.title)} →</a>`:'<a href="practice.html">Mixed practice →</a>'}</div></article></div>`;write(file(l),page(l.title,content,file(l),`<script>window.LESSON_ID=${JSON.stringify(l.id)};</script>`));}
 write('practice.html',page('Practice lab',`<div class="page-intro"><p class="eyebrow">SEE IT · ATTEMPT IT · EXPLAIN IT</p><h1>Practice lab.</h1><p class="lead">${C.drills.length} positions with recorded answers. Work slowly enough to describe what matters before you move.</p></div><div class="filters"><label>Skill<select id="theme-filter"><option value="">All skills</option></select></label><label>Practice set<select id="practice-mode"><option value="all">All exercises</option><option value="new">Not yet attempted</option><option value="due">Due for review</option></select></label><span id="practice-stats" class="micro" role="status"></span></div><div id="empty-practice" class="empty" hidden>No exercises in this set yet. Choose All exercises or a different skill.</div><section id="practice-content"><div class="practice-layout"><div class="board-wrap"><p class="micro" id="turn-label"></p>${boardFrame('<div class="board" id="practice-board"></div>')}<div class="board-controls"><button class="secondary" id="flip-board">Flip board</button><button class="secondary" id="reset-board">Reset board</button><a class="micro" id="analysis-link" target="_blank" rel="noopener">Explore on Lichess ↗</a></div><p class="micro">Move exercises: select a piece, then a destination. Or enter SAN (Nf3) or coordinates (g1f3). Use arrow keys between squares.</p></div><div class="exercise-panel"><p class="eyebrow" id="question-theme"></p><p class="micro" id="exercise-count"></p><h2 class="question" id="question"></h2><div class="choice-list" id="choice-list"></div><div class="move-entry" id="move-entry"><label>Move<input id="move-input" autocomplete="off" spellcheck="false" placeholder="Nf3 or g1f3"></label><label>Promotion<select id="promotion"><option value="q">Queen</option><option value="r">Rook</option><option value="b">Bishop</option><option value="n">Knight</option></select></label><button id="submit-move">Try move</button></div><div class="actions"><button class="secondary" id="hint-button">Give me a hint</button><button class="secondary" id="reveal-button">Reveal & review later</button></div><p class="hint" id="hint-text" role="status"></p><div class="feedback" id="feedback" role="status" aria-live="polite"></div><p id="explanation" hidden></p><p class="line" id="solution-line" hidden></p><button class="secondary" id="next-line" hidden>Play next move in the line</button></div></div><div class="practice-footer"><button class="secondary" id="previous-exercise">← Previous</button><button id="next-exercise">Next exercise →</button></div></section><section class="callout"><h3>How review works</h3><p>The first submitted answer records the attempt. A miss or reveal stays due now; a correct first attempt schedules another review after 1, 3, 7, 14, then 30 days as your streak grows. Hints are allowed. This is a simple practice schedule, not a rating or a prediction of mastery.</p><p><a href="progress.html">Export your progress</a> · <a href="sources.html">Read how answers were checked</a></p></section>`,'practice.html'));
 const weeks=Array.from({length:6},(_,i)=>{const ls=C.lessons.slice(i*4,i*4+4);return `<section class="card week"><strong>WEEK ${i+1}</strong><div><h3>${i===0?'Build the routine':i===5?'Combine and revisit':`Lessons ${i*4+1}–${i*4+4}`}</h3><p>${ls.map(l=>`<a href="${file(l)}">${esc(l.title)}</a>`).join(' · ')}</p><p>Use four short sessions for these lessons. Do 4–6 practice positions after each session. Play one slow game, then review two decisions connected to the week’s ideas. Revisit earlier misses before adding more positions.</p></div></section>`;}).join('');
 write('study-plan.html',page('Six-week study plan',`<div class="page-intro"><p class="eyebrow">A PACE YOU CAN REPEAT</p><h1>Learn a little.<br>Use it often.</h1><p class="lead">A suggested six-week route through ${C.title}. Aim for roughly two to three hours a week, including a slow game. Stretch the schedule whenever accuracy or available time calls for it.</p></div><section class="callout"><h3>A 20-minute session</h3><p>3 minutes: revisit one old miss. 7 minutes: read a lesson and attempt its task. 7 minutes: solve a few unfamiliar positions. 3 minutes: write the cue you want to recognize in a game.</p></section>${weeks}<section class="prose"><h2>A useful graduation check</h2><p>Choose twelve mixed positions without looking at theme labels. Explain your answer and the opponent’s relevant resource. Review the misses and repeat them after a delay. Then inspect one slow game for examples of the same skill. Course completion records your study; it does not certify a playing strength.</p><h2>If the material feels too hard</h2><p>Shorten the task. Use a physical board, solve untimed, and explain just the next move or structural fact. Return to the relevant earlier lesson when mistakes repeat. Move diagrams physically only after attempting the mental step you are practicing.</p><h2>Across the full project</h2><p>The four six-week courses form a 24-week route. You can also use them selectively: board vision supports calculation, planning gives quiet moves a purpose, and endgames teach what earlier exchanges are aiming for.</p></section>${seriesHTML}`,'study-plan.html'));
 write('progress.html',page('Your progress',`<div class="page-intro"><p class="eyebrow">YOUR OWN STUDY RECORD</p><h1>Keep the useful part.</h1><p class="lead">Lesson completion, exercise review dates, and your notes are stored on this browser for ${C.title}. There is no login, cloud synchronization, or collection of your notes by this site.</p></div>${progress}<div class="grid"><section class="card"><h3>Back up your learning</h3><p>Export a JSON file before changing devices, clearing browser storage, or resetting this course. Imports only accept a backup for this course.</p><button data-export>Export progress & notes</button><label class="journal-label" for="import-progress">Import a backup</label><input id="import-progress" type="file" accept=".json,application/json"><p id="import-status" role="status" class="status"></p></section><section class="card"><h3>Start this course again</h3><p>Resetting clears only this course’s local completion marks, exercise records, and notes. Keep an export if you want to return to them.</p><button class="secondary" id="reset-progress">Reset this course’s progress</button></section></div><div class="actions"><a class="button" href="practice.html">Return to practice →</a><a class="button secondary" href="index.html">Lesson directory</a></div>`,'progress.html'));
 write('downloads.html',page('Downloads',`<div class="page-intro"><p class="eyebrow">FOR A DESK, A CHESSBOARD, OR ANOTHER TOOL</p><h1>Take the course with you.</h1><p class="lead">Read the lessons offline, print a workbook, or load the positions into a chess program. The answer data is included so the course does not depend on a live engine service.</p></div><div class="download-grid"><section class="card"><h3>Printable workbook</h3><p>All 24 lessons, written practice tasks, space for notes, and an answer section. Open it, then choose Print → Save as PDF if you want a PDF copy.</p><a class="button" href="workbook.html">Open workbook</a></section><section class="card"><h3>Plain-text course notes</h3><p>A Markdown edition of the full written course, including tasks, explanations, and recall questions.</p><a class="button secondary" href="downloads/course-notes.md" download>Download notes (.md)</a></section><section class="card"><h3>Chess positions in PGN</h3><p>Every practice position with a FEN header and the recorded answer or line. Import into a program that supports setup positions.</p><a class="button secondary" href="downloads/exercises.pgn" download>Download positions (.pgn)</a></section><section class="card"><h3>Structured exercise data</h3><p>Questions, FENs, accepted answers, explanations, and verification details for all ${C.drills.length} exercises.</p><a class="button secondary" href="data/exercises.json" download>Download exercises (.json)</a></section></div><section class="prose"><h2>The whole offline site</h2><p>On the <a href="${repo}">GitHub repository</a>, choose Code → Download ZIP. Extract it and open <code>index.html</code>. All required scripts and styles are included locally. External source and analysis links require an internet connection.</p><h2>Your personal progress</h2><p>Use <a href="progress.html">Your progress</a> to export notes and exercise history. Downloading the course files does not automatically include browser-stored progress.</p></section>`,'downloads.html'));
 const verification=C.repo==='chess12'?`The 120 tactical exercises came from original simulated legal games. Stockfish 19 analyzed each selected position with MultiPV 3 and a 200,000-node search. The selected first move had a substantial score gap in that recorded search. This is finite engine analysis, not an exhaustive proof of uniqueness; another legal move may still be playable. The stored principal variation is one continuation, not a claim that every reply is forced.`:C.repo==='chess14'?`All 96 endgame exercises were checked against the Lichess Syzygy tablebase service. Win, draw, and loss are from the side-to-move perspective. Accepted move answers preserve the recorded result. The diagrams use a zero halfmove clock and no invented repetition history. The saved move categories make the answers available offline.`:`All 144 exercises use original positions reached through legal moves. Answers are deterministic counts computed from chess.js attack maps, legal moves, or pawn locations. Attack-map questions explicitly count geometric attacks, including pinned pieces; legal-move questions separately enforce king safety. A structural count is not a claim that the position is strategically won.`;
 write('sources.html',page('Sources and verification',`<div class="page-intro"><p class="eyebrow">CLEAR SCOPE, CHECKABLE ANSWERS</p><h1>What this course can claim.</h1><p class="lead">A complete study route through its stated practical foundations, with exercises and a review method. It is not an encyclopedia of every chess position or a promise of a particular rating.</p></div><article class="prose"><h2>Original teaching material</h2><p>The lessons and site were created with AI for knightway8’s Human Chess Project. The explanations are original instructional writing. Practice positions were generated for this collection rather than copied from another author’s annotated lessons. Human learning remains the focus: inspect, attempt, explain, and use the idea in a real game.</p><h2>How this practice set was checked</h2><p>${verification}</p><p><a href="VALIDATION.json">Machine-readable validation summary</a> · <a href="data/exercises.json">Full recorded exercise data</a></p><h2>References and tools</h2><ul><li><a href="https://handbook.fide.com/chapter/E012023">FIDE Laws of Chess</a>: authoritative rules for legal moves and over-the-board play. Tournament and platform procedures can differ.</li><li><a href="https://github.com/jhlywa/chess.js">chess.js 1.4.0</a>: legal move generation, attack maps, and board state. Its BSD-2-Clause license is included in <a href="vendor/chess-LICENSE.txt">the repository</a>.</li><li><a href="https://stockfishchess.org/">Stockfish</a>: Stockfish 19 was used locally to generate and analyze the tactical practice set. The engine executable is not needed by this website and is not bundled.</li><li><a href="https://github.com/lichess-org/lila-tablebase">Lichess tablebase server and API documentation</a>: exact small-piece endgame results used in the endgame course.</li><li><a href="https://lichess.org/practice">Lichess Practice</a>: optional further practice with an independent interface. This course does not copy its lesson prose.</li></ul><h2>Board artwork</h2><p>Colin M. L. Burnett’s Cburnett vector pieces are distributed unmodified from Lichess under GPL version 2 or later. The clear outlines, cream and olive squares, and outside coordinates follow the supplied design reference. <a href="assets/pieces/README.md">Artwork credits and source</a> · <a href="assets/pieces/COPYING.txt">Full license</a>.</p><h2>Read the limits correctly</h2><p>A legal position is not necessarily a strategically sensible one. Some exercises isolate elementary skills in unusual positions. A count or engine answer does not replace an explanation of the mechanism. The course distinguishes rule-based counts, finite engine analysis, and exact tablebase results rather than presenting them as the same kind of evidence.</p><h2>Report a correction</h2><p>Open an issue in <a href="${repo}/issues">this repository</a> with the lesson or exercise ID, the position or FEN, the claimed problem, and a proposed correction or line. Avoid personal information in public issues. Updates should use pull requests so the protected branch keeps its history.</p><h2>Browser storage</h2><p>Progress stays under a separate key for this course. There are no analytics, remote fonts, or external scripts. If browser storage is unavailable, use the export control to keep the current session’s record.</p></article>`,'sources.html'));
 const chapters=C.lessons.map(l=>`<section class="chapter"><p class="eyebrow">LESSON ${String(l.number).padStart(2,'0')} · ${C.modules[l.module]}</p><h2>${esc(l.title)}</h2><p><strong>Goal:</strong> ${esc(l.goal)}</p><p>${esc(l.teach)}</p><h3>Worked explanation</h3><p>${esc(l.example)}</p>${l.diagram?`<figure class="model-diagram">${staticBoard(l.diagram.fen)}<figcaption>${esc(l.diagram.caption)}</figcaption></figure>`:''}<h3>Your task</h3><p>${esc(l.task)}</p><div class="worksheet" aria-label="Space for your answer"></div><p><strong>Watch for:</strong> ${esc(l.trap)}</p><p><strong>Game transfer:</strong> Record a move or position from your own game where this idea matters.</p><div class="worksheet"></div></section>`).join('');
 const answerKey=C.lessons.map(l=>`<section><h3>${String(l.number).padStart(2,'0')}. ${esc(l.title)}</h3><p>${esc(l.answer)}</p><p><strong>Recall:</strong> ${esc(l.recall)}</p></section>`).join('');
 write('workbook.html',`<!doctype html><html lang="en" style="--accent:${C.color}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${C.title} — printable workbook</title><link rel="stylesheet" href="assets/style.css"></head><body><main class="printbook"><div class="no-print actions"><a class="button secondary" href="downloads.html">← Downloads</a><button id="print-book">Print or save as PDF</button></div><p class="eyebrow">THE HUMAN CHESS PROJECT · ${C.repo}</p><h1>${C.title}<br>Study workbook</h1><p class="lead">${C.subtitle}</p><p>24 lessons · Written exercises · Answer key · Six-week route</p><p>Attempt each task before reading its answer. Use a physical board whenever it helps you check a line. Online practice: <a href="${pages}">${pages}</a></p><p>Suggested pace: four lessons a week, several short practice sessions, and one slow game with review. Use the answer key to diagnose an error, not to skip the attempt.</p>${chapters}<section class="answer-key"><h2>Answers and recall prompts</h2>${answerKey}</section></main><script>document.getElementById('print-book').addEventListener('click',()=>window.print());</script></body></html>`);
 write('404.html',page('Page not found',`<div class="page-intro"><p class="eyebrow">LET’S FIND YOUR PLACE</p><h1>That page isn’t here.</h1><p class="lead">Return to the lesson directory or practice lab.</p><div class="actions"><a class="button" href="${pages}">Course home</a><a class="button secondary" href="${pages}practice.html">Practice lab</a></div></div>`,'404.html'));
 write('downloads/course-notes.md',`# ${C.title}\n\n${C.subtitle}\n\nLive course: ${pages}\n\n${C.lessons.map(l=>`## ${String(l.number).padStart(2,'0')}. ${l.title}\n\n**Goal:** ${l.goal}\n\n${l.teach}\n\n### Worked explanation\n\n${l.example}\n\n### Try it yourself\n\n${l.task}\n\n### Answer\n\n${l.answer}\n\n**Common mistake:** ${l.trap}\n\n**Recall:** ${l.recall}\n`).join('\n')}`);
 write('README.md',`# ${C.title}\n\n**[Open the live GitHub Pages course](${pages})**\n\n${C.subtitle}. ${C.promise}\n\n## Who it is for\n\n${C.audience}\n\nThis is an extensive course through a defined practical skill area, not a claim to cover all of chess. The full four-course route is aimed at human learners building reliable habits from the basics toward club play.\n\n## What is included\n\n- 24 distinct lessons in four modules, each with an explanation, task, answer, common mistake, and recall prompt.\n- ${C.drills.length} interactive exercises with saved answer data.\n- A six-week study plan and links to the full 24-week route.\n- Browser-local progress, a review queue, private study notes, and progress import/export.\n- A printable workbook with an answer key, Markdown notes, PGN positions, and structured JSON exercises.\n- Offline scripts and styles; no account, analytics, remote fonts, or runtime engine service required.\n\n## Modules\n\n${C.modules.map((m,i)=>`${i+1}. ${m}`).join('\n')}\n\n## Start here\n\n[Lesson directory](${pages}) · [Practice lab](${pages}practice.html) · [Study plan](${pages}study-plan.html) · [Downloads](${pages}downloads.html) · [Sources and verification](${pages}sources.html)\n\n## The four-course collection\n\n| Repository | Course | Live site |\n| --- | --- | --- |\n${series.map(([n,t])=>`| [${n}](https://github.com/knightway8/${n}) | ${t} | [Open](https://knightway8.github.io/${n}/) |`).join('\n')}\n\n## Use offline\n\nChoose **Code → Download ZIP**, extract the archive, and open \`index.html\` in a browser. External reference and analysis links require internet access. Progress is browser-local; export it before clearing storage or changing devices.\n\n## Verification\n\n${verification}\n\nSee [VALIDATION.json](VALIDATION.json) for check results. The original prose is AI-created educational material; report a concrete correction with its lesson or exercise ID. No rating improvement is promised.\n\n## Maintain and publish\n\nThe editable source is [data/course.json](data/course.json). Run \`node tools/build.mjs\` to regenerate the pages, then \`node tools/validate.cjs\` for content, answer-legality, PGN, and link checks. The browser data and downloadable files are regenerated from the same source.\n\nGitHub Pages serves the root of \`main\` with \`.nojekyll\`. Active branch rules require pull requests, prevent force pushes, and prevent deleting the default branch, with no bypass actors. Repository owners can still change settings or delete the repository.\n\n## Credits\n\nOriginal course writing, interface, and generated practice positions were prepared with AI for knightway8. Cburnett SVG chess pieces by Colin M. L. Burnett are included unmodified under GPL-2.0-or-later, with [source and attribution](assets/pieces/README.md) and the [full license](assets/pieces/COPYING.txt). The bundled chess.js library is BSD-2-Clause licensed; its full notice is in [vendor/chess-LICENSE.txt](vendor/chess-LICENSE.txt). Stockfish was used for local analysis and is not redistributed here. Tablebase facts are credited to the Lichess Syzygy service.\n`);
 write('CONTRIBUTING.md',`# Improving this course\n\nOpen an issue with a lesson or exercise ID, FEN where relevant, the disputed claim, and a concrete correction. Preserve unrelated content.\n\nEdit data/course.json, run node tools/build.mjs, and run node tools/validate.cjs. Check the changed pages in a browser. Keep source answers and explanations consistent; changing an engine or tablebase answer requires new recorded evidence.\n\nUse a branch and pull request. Review the complete diff before merging. Keep default-branch protections and Pages settings enabled. After merging, confirm the Pages deployment succeeds and the affected lesson works.\n`);
 write('.nojekyll','');write('.gitattributes','* text=auto\n');write('.gitignore','.DS_Store\nThumbs.db\n');
 const names=['index.html','practice.html','study-plan.html','downloads.html','progress.html','sources.html','workbook.html',...C.lessons.map(file)];
 write('sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${names.map(n=>`<url><loc>${pages}${n==='index.html'?'':n}</loc></url>`).join('')}</urlset>`);write('robots.txt',`User-agent: *\nAllow: /\nSitemap: ${pages}sitemap.xml\n`);
 fs.writeFileSync(path.join(root,'data/exercises.json'),JSON.stringify(C.drills,null,2)+'\n');
 const runtime={...C,drills:C.drills.map(({analysis,history,source,...d})=>d)};fs.writeFileSync(path.join(root,'assets/data.js'),'window.COURSE='+JSON.stringify(runtime).replaceAll('<','\\u003c')+';\n');
 const pgn=C.drills.map(d=>{const header=`[Event "${C.title}: ${d.id}"]\n[Site "${pages}practice.html?id=${d.id}"]\n[Date "2026.09.05"]\n[Round "-"]\n[White "Human learner"]\n[Black "Practice position"]\n[Result "*"]\n[SetUp "1"]\n[FEN "${d.fen}"]`;
 let text=`{ ${d.prompt.replaceAll(/[{}]/g,'')} Answer: ${(d.kind==='move'?d.answers.join(', '):d.answers[0]).replaceAll(/[{}]/g,'')}. ${d.explanation.replaceAll(/[{}]/g,'')} }`;
 if(d.line){let side=d.fen.split(' ')[1],num=+d.fen.split(' ')[5];for(const m of d.line){text+=' '+num+(side==='w'?'.':'...')+' '+m.san;if(side==='b')num++;side=side==='w'?'b':'w';}}
 return header+'\n\n'+text+' *\n';}).join('\n');write('downloads/exercises.pgn',pgn);
}
const here=fileURLToPath(import.meta.url);if(process.argv[1]&&path.resolve(process.argv[1])===here){const root=path.resolve(path.dirname(here),'..');const C=JSON.parse(fs.readFileSync(path.join(root,'data/course.json'),'utf8'));renderSite(C,root);console.log(`Rebuilt ${C.repo}: ${C.lessons.length} lessons, ${C.drills.length} exercises.`);}
